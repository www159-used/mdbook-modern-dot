name: Docs

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6

      - uses: ts-graphviz/setup-graphviz@v2

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.92.0

      - uses: swatinem/rust-cache@v2

      - name: Install mdBook
        run: cargo install mdbook --locked --force

      - name: Build preprocessor
        run: cargo build --release -p mdbook-modern-dot

      - name: Build book
        run: |
          sed -i 's|command = "cargo run -q -p mdbook-modern-dot"|command = "../target/release/mdbook-modern-dot"|' book/book.toml
          mdbook build book

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: book/book/html

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
